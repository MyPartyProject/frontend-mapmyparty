import SupportWorkspace from "@/components/support/SupportWorkspace";

const PromoterSupport = () => {
  return (
    <SupportWorkspace
      mode="staff"
      title="Support Queue"
      description="Unified operational queue for promoter, admin, and superadmin ticket triage."
    />
  );
};

export default PromoterSupport;
